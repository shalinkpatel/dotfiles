# Copy to ~/.profile.secret (same dir as $HOME, NOT inside the repo) and fill
# in real values. ~/.profile sources this at the end; it is never committed.
# Keep the real file mode 600.

export BASETEN_API_KEY=""
